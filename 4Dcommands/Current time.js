// 4D command: Current time
// Returns the current system time
// Current time -> Function result
// Function result		Time		Current system time

export default function Current_time(processState) {
    // Get current time
    const now = new Date();
    return now;
}