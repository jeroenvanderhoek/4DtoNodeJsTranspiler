// 4D command: Length
/**
 * 
 * @param {*} processState 
 * @param {string} str string
 * @returns {number} Length of string
 */
export default function Length (processState,str) {
    return str.length;
}