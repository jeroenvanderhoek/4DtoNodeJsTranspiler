// 4D command: ADD RECORD

export default async function ADD_RECORD (processState) {

    //FIXME

    // const client = await pool.connect();  
    // try {  
    //     const res = await client.query('INSERT INTO Table (Field) VALUES ($1) RETURNING *', ['value']);  
    //     return res.rows[0];  
    // } finally {  
    //     client.release();  
    // }  


}

