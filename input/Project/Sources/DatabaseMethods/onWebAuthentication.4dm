//4DACTION URL, a 4DSCRIPT tag, etc.).

#DECLARE($vt_url : Text; $vt_http_header : Text; $vt_ip_adress_web_client : Text; $vt_ip_adress_server : Text; $vt_username : Text; $vt_password : Text)

// WEB SEND TEXT:C677("Authorized.")